# Auditoría RLS por rol — go-live

Fecha base: 2026-09-11 · Actualizado go-live Etapa 1.

## Método de verificación

Para cada celda: sesión real del rol + intento de lectura/escritura cruzada.
Marcar `OK` solo con evidencia (navegador o script autenticado).

## Matriz

| Tabla / RPC | admin | docente | alumno | coordinador | comunidad | lead | anon |
|---|---|---|---|---|---|---|---|
| `users` (propio) | OK write | OK read/own | OK read/own | OK read/own | OK read/own | OK read/own | deny |
| `users` (ajeno) | OK | deny | deny | deny | deny | deny | deny |
| `courses` publicados | OK | OK read | OK read | OK read | OK read | OK read | OK si publica |
| `courses` borrador propio | OK | OK | deny | deny | deny | deny | deny |
| `enrollments` propios | OK | read curso | OK | coord curso | OK | — | deny |
| `evaluation_attempts` propios | OK | corrección | OK own | — | OK own | — | deny |
| `certificates` + `verify_certificate` | OK | — | OK own | — | OK own | — | RPC only |
| `points_log` | OK read | — | OK own insert via RPC | — | OK own | — | deny |
| `bookings` propios | OK | — | OK | OK | OK | OK via CU-06 | deny |
| `bookings` ajenos | OK | deny | deny | deny | deny | deny | deny |
| `get_occupied_slots` | OK | OK | OK | OK | OK | OK | OK |
| `payments` | OK | deny | deny | deny | deny | deny | deny |
| `feature_flags` write | OK | deny | deny | deny | deny | deny | deny |
| `course_careers` | OK all | OK select | OK select | OK select | OK select | OK select | OK select |

## Invariantes (código)

- Policies admin: `is_admin()` security definer — nunca subquery a `public.users`.
- `points_log` append-only (triggers).
- Certificados públicos: solo `verify_certificate(uuid)`.
- Webhook MP: service role + `x-signature`.

## Pendiente humano (Etapa 6)

- [ ] Re-ejecutar matriz en preview con cuentas QA por rol
- [ ] Rotar `SUPABASE_SERVICE_ROLE_KEY`
